# Dedicated PostgreSQL database for integration tests only.
# Copy this file to .env.test locally and replace the placeholders. Do not
# commit .env.test or use a development, shared, or production database.
TEST_DATABASE_URL=postgresql+psycopg://postgres:your-password@localhost:5432/url_shortener_test
